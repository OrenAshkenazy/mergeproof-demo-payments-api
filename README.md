# mergeproof-demo-payments-api

Safe demo app for MergeProof production-readiness runs.

The base branch owns:

- `mergeproof.yaml`
- capability packs under `capabilities/`
- a small app skeleton

Create the demo app PR by adding the files from `../app-pr-files/`.
# webhook trigger 2
